<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hsgene.dao.EmployeeDao">

    <select id="findList" resultType="Employee">
        SELECT
         e.id AS "id",
         e.name AS "name",
         e.sex AS "sex",
         e.job AS "job",
         e.identity_card AS "identityCard",
         e.dept_no AS "deptNo",
         e.create_date AS "createDate"
         FROM employee e
    </select>

    <select id="getById" resultType="Employee">
        select * from employee e where id = #{id}
    </select>

    <!-- 加上useGeneratedKeys="true"和keyProperty="id"属性，insert语句执行后会将自增长的id覆盖赋给传入的对象 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO employee(
        name,
        sex,
        job,
        access_level,
        identity_card,
        dept_no,
        create_date
        )
        VALUES (
        #{name},
        #{sex},
        #{job},
        #{accessLevel},
        #{identityCard},
        #{deptNo},
        #{createDate}
        )
    </insert>

    <update id="update">
        UPDATE employee SET
        name = #{name},
        dept_no = #{deptNo}
        WHERE id = #{id}
    </update>

    <delete id="delete">
      DELETE FROM employee WHERE id = #{id}
    </delete>

    <!-- 使用foreach标签可以在insert、update、delete语句中进行批量增删改 -->
</mapper>