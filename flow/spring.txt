mybatis交给spring管理
spring容器即spring上下文就是一个大的类，所有的配置在其中的类都是作为它的的属性，
通过spring管理就是为这些属性实例化初始化，开发使用时只需要提供spring上下文即可获得其中的任意属性，
在项目启动时spring配置文件中的对象有些会直接被实例化，作为单例使用，
交给spring管理后所有bean的实例化都可以由spring提供的注解或api来完成